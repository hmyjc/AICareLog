# 🚀 Taro 多端项目启动指南

## 方式一：使用启动脚本（推荐）

### Windows 用户

#### 启动 H5 开发（浏览器预览）
双击运行：`start_taro_h5.bat`

或在命令行中：
```cmd
cd D:\PythonProject\heathy_agent\health_agent
start_taro_h5.bat
```

#### 启动微信小程序开发
双击运行：`start_taro_weapp.bat`

或在命令行中：
```cmd
cd D:\PythonProject\heathy_agent\health_agent
start_taro_weapp.bat
```

### Linux/Mac 用户

#### 启动 H5 开发
```bash
cd /path/to/health_agent
chmod +x start_taro_h5.sh
./start_taro_h5.sh
```

#### 启动微信小程序开发
```bash
cd /path/to/health_agent
chmod +x start_taro_weapp.sh
./start_taro_weapp.sh
```

---

## 方式二：手动命令

### 1. 进入项目目录
```bash
cd frontend-taro
```

### 2. 安装依赖（首次运行）
```bash
pnpm install
```

如果没有安装 pnpm，先安装：
```bash
npm install -g pnpm
```

### 3. 启动开发服务器

#### H5 开发（浏览器）
```bash
pnpm dev:h5
```
浏览器访问：http://39.104.28.40:3000

#### 微信小程序开发
```bash
pnpm dev:weapp
```
然后使用微信开发者工具打开 `frontend-taro/dist` 目录

#### 其他平台
```bash
pnpm dev:alipay    # 支付宝小程序
pnpm dev:tt        # 字节跳动小程序
pnpm dev:qq        # QQ小程序
pnpm dev:rn        # React Native
```

---

## 🔧 环境检查

启动前确保已安装：
- ✅ Node.js >= 16 （运行 `node -v` 检查）
- ✅ pnpm >= 8 （运行 `pnpm -v` 检查）

---

## 📱 微信小程序额外步骤

1. 下载并安装 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2. 运行 `pnpm dev:weapp` 编译项目
3. 打开微信开发者工具
4. 选择"导入项目"
5. 项目目录选择：`frontend-taro/dist`
6. AppID 选择"测试号"或填写您的 AppID
7. 点击"导入"

---

## ❓ 常见问题

### Q1: pnpm 命令找不到？
**A:** 安装 pnpm：
```bash
npm install -g pnpm
```

### Q2: 依赖安装失败？
**A:** 尝试清理缓存后重新安装：
```bash
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

### Q3: 端口 3000 已被占用？
**A:** 修改 `config/index.ts` 中的端口号：
```typescript
h5: {
  devServer: {
    port: 3001  // 改为其他端口
  }
}
```

### Q4: 微信开发者工具导入失败？
**A:** 确保：
- 已运行 `pnpm dev:weapp` 生成 dist 目录
- 导入的是 `frontend-taro/dist` 而不是 `frontend-taro`
- 微信开发者工具版本为最新

---

## 📚 更多帮助

- 快速入门：阅读 `QUICKSTART.md`
- 详细指南：阅读 `UNI_TO_TARO_GUIDE.md`
- 项目说明：阅读 `README.md`

祝您开发顺利！🎉

