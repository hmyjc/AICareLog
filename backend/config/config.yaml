# 阿里云百炼配置
dashscope:
  url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  api_key: "sk-058822419ec34e57b24057ad219d45ea"
  model: "qwen-max"

# MongoDB配置
mongodb:
  uri: "mongodb://root:Jchmy2025@dds-bp18df879a0455c41679-pub.mongodb.rds.aliyuncs.com:3717/admin"
  database: "health-agent"  # 使用你新建的数据库
  collection_health_profile: "health_profiles"  # 健康档案集合
  collection_push_history: "push_history"  # 推送历史集合
  collection_persona_styles: "persona_styles"  # 人物风格集合（预留）

# 服务器配置
server:
  host: "0.0.0.0"
  port: 8000
  debug: true

# 定时任务配置
scheduler:
  timezone: "Asia/Shanghai"
  # 作息提醒时间
  rest_times:
    - "07:00"  # 早起提醒
    - "13:00"  # 午睡提醒
    - "23:00"  # 睡觉提醒
  # 饮食提醒时间
  meal_times:
    - "07:30"  # 早餐
    - "12:00"  # 午餐
    - "18:00"  # 晚餐
  # 天气推送时间
  weather_time: "07:00"
  # 养生妙招推送时间
  health_tip_time: "14:00"

